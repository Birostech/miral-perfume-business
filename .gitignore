# Node modules (installed fresh during CodeBuild)
node_modules/
lambda/api/node_modules/

# Build artifacts (generated by buildspec.yml)
*.zip

# Environment / secrets — never commit these
.env
.env.local
.env.production
secrets.json

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Claude Code settings (local only)
.claude/settings.local.json
